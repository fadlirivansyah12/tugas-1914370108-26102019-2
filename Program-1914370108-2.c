#include <stdio.h>
int main()
{
    int a,b,c;
    if (a > 0){
        printf ("D= b*2 - (4*a*c)")
    } else {
    if ("D>=0"){
        printf ("x1=(-b+√D)/2*a");
        scanf ("x2=(-b+√D)/2*a");
        printf ("print x1,x2");
     printf("print akar imajiner");
    }
    return 0;
  }}
